{
  "name": "darmcamp",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "npm run build && npx serve out",
    "build": "node scripts/build.cjs",
    "start": "npx serve out"
  },
  "devDependencies": {
    "serve": "^14.2.4"
  }
}
